# @viteplus/versions declares peer vitepress "^1.6.4 || ^2.0.0", which semver
# does not match against our 2.0.0-alpha.16. The build was verified working on
# that combination, so accept the peer mismatch instead of downgrading VitePress.
legacy-peer-deps=true
